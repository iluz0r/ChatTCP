1) Code clean (with processResp() methods, attributes in ClientHandler, names, etc)
2) Change the string separator ':';
3) Fix the list refresh;
4) Add the scroll to the textArea chats;
5) Improve the graphics
